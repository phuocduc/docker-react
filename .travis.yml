sudo: required
services: 
    - docker

before-install:
    - docker build -t ducnguyen095456/dockerReact -f Dockerfile.dev .  # chay file dockerfile.dev vs cai tag name phuocduc/docker-react

script:
    - docker run ducnguyen095456/dockerReact npm run test -- --coverage # run test
